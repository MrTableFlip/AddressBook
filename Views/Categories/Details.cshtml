@model AddressBook.Models.Category
@using AddressBook.Services
@inject IImageService _imageService
@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Category</h4>
    <hr />
    <div class="row">
        <div class="col">
            <img id="CatPic" class="img-fluid" src="@_imageService.DecodeImage(Model.CatPic, Model.ContentType)" />
        </div>
        <div class="col">
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.CatId)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.CatId)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Name)
                </dd>
            </dl>
        </div>
    </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
